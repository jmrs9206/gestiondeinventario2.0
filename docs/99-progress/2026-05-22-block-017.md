# 2026-05-22-block-017.md

    > Proyecto: VDEnergy Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Bloque

```text
BLOCK-017-public-api
```

## Tareas realizadas

- Creadas entidades JPA `ApiClient.java` y `ApiClientScope.java` mapeando las tablas correspondientes del esquema de base de datos.
- Creado `ApiClientRepository.java` para buscar clientes activos por el hash SHA-256 de su API Key.
- Creado `ApiKeyAuthenticationToken.java` y el filtro de servlet `ApiKeyFilter.java` para leer la cabecera `X-API-Key`, verificar el cliente y propagar los scopes autorizados como authorities de Spring Security.
- Diseñada la clase de configuración de seguridad `PublicApiSecurityConfig.java` registrando un `SecurityFilterChain` exclusivo y sin estado para `/public-api/**` con prioridad alta (`@Order(1)`), respetando la encapsulación del módulo.
- Implementado `PublicApiService.java` para coordinar el hasheado de llaves, la validación y el registro del evento `PUBLIC_API_ACCESS` en la auditoría global.
- Creado `PublicApiController.java` exponiendo los endpoints `POST /public-api/v1/users` (requiere scope `users:create`) y `GET /public-api/v1/materials` (requiere scope `materials:read`).
- Escrita batería de pruebas de integración `PublicApiControllerTest.java` para verificar la validación de cabeceras, denegación por scopes incorrectos, rechazo de clientes inactivos y auditoría automática de accesos.

## Validación

```text
mvn clean verify OK - 71/71 tests pasaron de forma limpia.
```

## Siguiente bloque

```text
BLOCK-018-nextjs-bootstrap.md
```

    ---

    ## Regla final IA

    La IA debe trabajar solo con el alcance explícito de este archivo, no debe inventar funcionalidad fuera del bloque o documento activo, y debe registrar progreso en `docs/99-progress/` cuando complete trabajo real.
