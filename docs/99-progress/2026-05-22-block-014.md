# 2026-05-22-block-014.md

    > Proyecto: StockFlow Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Bloque

```text
BLOCK-014-audit-log
```

## Tareas realizadas

- Creada entidad `AuditLog.java` mapeando la tabla `audit_log` en `com.stockflow.inventory.audit.entity`.
- Creado `AuditLogRepository.java` en `com.stockflow.inventory.audit.repository`.
- Actualizado `AuditService.java` para persistir eventos de auditoría en base de datos. Trunca el campo `userAgent` a un máximo de 500 caracteres para evitar errores de truncado y soporta el registro de campos `oldValue`/`newValue` como texto JSON.
- Creado `AuditServiceTest.java` verificando el guardado de eventos, truncado de user-agent y almacenamiento de valores JSON antiguos/nuevos, garantizando la compatibilidad.

## Validación

```text
mvn test OK - 62/62 tests pasaron de forma limpia.
```

## Siguiente bloque

```text
BLOCK-015-dashboard-backend-kpis.md
```

    ---

    ## Regla final IA

    La IA debe trabajar solo con el alcance explícito de este archivo, no debe inventar funcionalidad fuera del bloque o documento activo, y debe registrar progreso en `docs/99-progress/` cuando complete trabajo real.
