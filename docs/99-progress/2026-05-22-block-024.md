# 2026-05-22-block-024.md

    > Proyecto: StockFlow Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Bloque

```text
BLOCK-024-ci-cd
```

## Tareas realizadas

- Creado el flujo de trabajo de integración continua (`.github/workflows/ci.yml`) que se ejecuta en eventos `push` y `pull_request` contra las ramas `main`/`master`.
- El flujo realiza pruebas en paralelo y valida:
  - **Backend CI**: Configuración de JDK 21 Temurin, caché de dependencias Maven, compilación y verificación de pruebas (`mvn clean verify`).
  - **Frontend CI**: Configuración de Node.js 20, caché de npm, instalación de dependencias limpia (`npm ci`), pruebas y compilación del empaquetado de producción (`npm run build`).
  - **Docker Build Check**: Verifica en paralelo la correcta construcción y empaquetado de las imágenes Docker del Backend y del Frontend, asegurando que no haya fallas de despliegue en entornos de contenedores.

## Validación

```text
GitHub Actions CI workflow compilado y estructurado correctamente bajo los estándares oficiales.
```

## Siguiente bloque

```text
BLOCK-025-production-deployment
```

    ---

    ## Regla final IA

    La IA debe trabajar solo con el alcance explícito de este archivo, no debe inventar funcionalidad fuera del bloque o documento activo, y debe registrar progreso en `docs/99-progress/` cuando complete trabajo real.
