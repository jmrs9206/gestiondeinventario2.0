# 2026-05-22-block-005.md

    > Proyecto: VDEnergy Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Fecha

```text
2026-05-22
```

## Bloque

```text
BLOCK-005
```

## Objetivo

Crear entidades base.

## Tareas realizadas

- Se creó la clase abstracta `@MappedSuperclass` `BaseEntity.java` que define el campo de borrado lógico `active` y las marcas de tiempo de auditoría `createdAt` y `updatedAt`.
- Se configuraron los callbacks de ciclo de vida JPA `@PrePersist` y `@PreUpdate` en `BaseEntity` para manejar automáticamente el establecimiento y actualización de las marcas de tiempo.
- Se crearon los enums globales `UserRole` (`ADMIN`, `TECNICO`) y `MaterialStatus` (`OPERATIVO`, `ROTO`, `EN_REPARACION`, `BAJA`).
- Se verificó que el proyecto compila y los tests pasan correctamente ejecutando `mvn clean compile` y `mvn test`.

## Archivos creados

```text
backend/src/main/java/com/vdenergy/inventory/common/entity/BaseEntity.java
backend/src/main/java/com/vdenergy/inventory/common/entity/UserRole.java
backend/src/main/java/com/vdenergy/inventory/common/entity/MaterialStatus.java
docs/99-progress/2026-05-22-block-005.md
```

## Archivos modificados

```text
docs/04-blocks/BLOCK-005-base-entities.md
```

## Tests ejecutados

```bash
mvn test
```

## Resultado

```text
SUCCESS
```

## Endpoints validados

```text
N/A
```

## Auditoría validada

```text
N/A
```

## Problemas encontrados

- Ninguno. Las dependencias JPA agregadas en el bloque anterior resolvieron y compilaron las anotaciones correctamente.

## Pendientes

- Ninguno.

## Siguiente bloque

```text
BLOCK-006
```

    ---

    ## Regla final IA

    La IA debe trabajar solo con el alcance explícito de este archivo, no debe inventar funcionalidad fuera del bloque o documento activo, y debe registrar progreso en `docs/99-progress/` cuando complete trabajo real.
