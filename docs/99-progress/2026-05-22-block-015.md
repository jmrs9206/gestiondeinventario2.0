# 2026-05-22-block-015.md

    > Proyecto: StockFlow Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Bloque

```text
BLOCK-015-dashboard-backend-kpis
```

## Tareas realizadas

- Creados DTOs `OfficeCountDto.java` y `DashboardKpisResponse.java` para estructurar los KPIs del dashboard de forma limpia.
- Creado `DashboardService.java` implementando el cálculo de métricas agregadas (total de materiales, recuentos por estado, materiales por oficina e incidencias activas).
- Añadida lógica en el servicio para calcular el tiempo medio de reparación en horas partiendo del historial en `material_history`.
- Creado `DashboardController.java` exponiendo el endpoint REST `GET /api/v1/dashboard/kpis` restringido únicamente al rol `ADMIN`.
- Creada batería de pruebas de integración y seguridad `DashboardControllerTest.java` logrando una cobertura completa del endpoint y el motor de cálculo.

## Validación

```text
mvn clean verify OK - 65/65 tests pasaron de forma limpia.
```

## Siguiente bloque

```text
BLOCK-016-dashboard-frontend.md
```

    ---

    ## Regla final IA

    La IA debe trabajar solo con el alcance explícito de este archivo, no debe inventar funcionalidad fuera del bloque o documento activo, y debe registrar progreso en `docs/99-progress/` cuando complete trabajo real.
