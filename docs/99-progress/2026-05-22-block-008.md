# 2026-05-22-block-008.md

    > Proyecto: VDEnergy Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Bloque

```text
BLOCK-008-roles-authorization
```

## Tareas realizadas

- Creado enum `Role.java` en `com.vdenergy.inventory.users.entity` que contiene los roles `ADMIN` y `TECNICO`.
- Modificada la entidad `User.java` para utilizar el nuevo enum `Role` en lugar del antiguo `UserRole`.
- Eliminado el archivo `UserRole.java` del paquete `com.vdenergy.inventory.common.entity`.
- Modificado `SecurityConfig.java` para habilitar la seguridad basada en métodos con `@EnableMethodSecurity` e inyectar `AuditService` en el `AccessDeniedHandler` personalizado.
- Implementado el registro de auditoría del evento `ACCESS_DENIED` cuando una solicitud autenticada es rechazada por no cumplir con los permisos de rol.
- Añadidos endpoints protegidos de prueba en `AuthController.java` (`/api/v1/auth/admin-only`, `/api/v1/auth/tecnico-only`, `/api/v1/auth/authenticated`) anotados con `@PreAuthorize` para validar los accesos según el rol asignado en el token JWT.
- Ampliada la suite de pruebas unitarias y de integración en `AuthControllerTest.java` para validar que:
  - Usuarios no autenticados reciben un código `401 Unauthorized`.
  - El rol `ADMIN` puede acceder a sus endpoints permitidos y genéricos pero es rechazado en los específicos de `TECNICO` (retornando `403 Forbidden` y registrando el evento `ACCESS_DENIED`).
  - El rol `TECNICO` puede acceder a sus endpoints permitidos y genéricos pero es rechazado en los específicos de `ADMIN` (retornando `403 Forbidden` y registrando el evento `ACCESS_DENIED`).

## Validación

```text
mvn test OK - Todos los 15 tests (los 12 originales y los 3 de autorización) ejecutados y pasados con éxito.
```

## Siguiente bloque

```text
BLOCK-009-users-module
```

    ---

    ## Regla final IA

    La IA debe trabajar solo con el alcance explícito de este archivo, no debe inventar funcionalidad fuera del bloque o documento activo, y debe registrar progreso en `docs/99-progress/` cuando complete trabajo real.
