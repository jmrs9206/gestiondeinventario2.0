# 2026-05-22-block-013.md

    > Proyecto: StockFlow Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Bloque

```text
BLOCK-013-material-history
```

## Tareas realizadas

- Actualizado `MaterialHistoryRepository.java` para extender `JpaSpecificationExecutor<MaterialHistory>` permitiendo filtros y búsquedas dinámicas en el historial de inventario.
- Creado `MaterialHistoryResponse.java` DTO en `com.stockflow.inventory.inventory.dto` proyectando datos limpios y sin IDs internos de base de datos (códigos públicos de materiales, nombres y correos de técnicos/usuarios realizadores, nombres y IDs públicos de las oficinas anteriores y nuevas).
- Creado `MaterialHistoryService.java` en `com.stockflow.inventory.inventory.service` implementando consultas dinámicas basadas en especificaciones de JPA (filtro opcional por material o acción) y aplicando un ordenamiento cronológico descendente predeterminado (más reciente primero).
- Creado `MaterialHistoryController.java` en `com.stockflow.inventory.inventory.controller` exponiendo la API bajo `/api/v1/inventory/history` y `/api/v1/inventory/materials/{publicCode}/history`, asegurados bajo el rol `ADMIN` o `TECNICO`.
- Creado `MaterialHistoryControllerTest.java` verificando autorizaciones por roles, listados generales y específicos por material con paginación, filtros dinámicos, control de errores (404 al consultar historial de un material inactivo o inexistente) y saneamiento de base de datos post-test para asegurar integridad referencial en ejecuciones subsecuentes.

## Validación

```text
mvn test OK - Todos los 59 tests pasaron con éxito de forma limpia.
```

## Siguiente bloque

```text
BLOCK-014-audit-log.md
```

    ---

    ## Regla final IA

    La IA debe trabajar solo con el alcance explícito de este archivo, no debe inventar funcionalidad fuera del bloque o documento activo, y debe registrar progreso en `docs/99-progress/` cuando complete trabajo real.
