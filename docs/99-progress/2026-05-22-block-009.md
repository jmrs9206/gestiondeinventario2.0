# 2026-05-22-block-009.md

    > Proyecto: StockFlow Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Bloque

```text
BLOCK-009-users-module
```

## Tareas realizadas

- Añadida la dependencia `spring-boot-starter-validation` a `pom.xml` para soportar validaciones automáticas mediante anotaciones.
- Creados DTOs de envoltura genéricos `ApiResponse.java` y `ApiPageResponse.java` en `com.stockflow.inventory.common.responses` para cumplir con las especificaciones de contrato de `API_CONTRACT_GUIDE.md`.
- Creadas excepciones personalizadas `ResourceNotFoundException.java` y `ConflictException.java` en `com.stockflow.inventory.common.exceptions`.
- Implementado `GlobalExceptionHandler.java` para capturar excepciones comunes (not found, conflict, validation) y formatear las respuestas de error en el formato oficial JSON.
- Creados DTOs del módulo de usuarios (`UserCreateRequest`, `UserUpdateRequest`, `UserStatusRequest`, `UserPasswordRequest`, `UserResponse`) bajo `com.stockflow.inventory.users.dto`.
- Creado `UserService.java` implementando las operaciones CRUD de administración de usuarios, la encriptación de contraseñas con BCrypt, y el control de sesiones (revocando refresh tokens al desactivar usuarios).
- Creado `UserController.java` exponiendo la API REST de usuarios bajo el prefijo `/api/v1/users` y protegido en su totalidad para el rol `ADMIN` mediante `@PreAuthorize("hasRole('ADMIN')")`.
- Integrada la auditoría para todos los eventos del ciclo de vida del usuario (`USER_CREATED`, `USER_UPDATED`, `USER_DISABLED`, `USER_ENABLED`, `PASSWORD_CHANGED`) a través de `AuditService`.
- Creada suite de pruebas unitarias y de integración en `UserControllerTest.java` para validar exhaustivamente los accesos por rol, las validaciones de campos, la prevención de correos duplicados, el ciclo de vida y la auditoría.

## Validación

```text
mvn test OK - Todos los 26 tests (los 15 previos y los 11 del módulo de usuarios) pasaron con éxito.
```

## Siguiente bloque

```text
BLOCK-010-offices-module
```

    ---

    ## Regla final IA

    La IA debe trabajar solo con el alcance explícito de este archivo, no debe inventar funcionalidad fuera del bloque o documento activo, y debe registrar progreso en `docs/99-progress/` cuando complete trabajo real.
