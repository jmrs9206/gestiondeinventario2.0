# DONE_BLOCK_002.md

    > Proyecto: StockFlow Inventory Management System  
    > Modo: Caveman Mode  
    > Fecha base: 2026-05-22

    ## Estado

```text
DONE
```

## Fecha

```text
2026-05-22
```

## Bloque

```text
BLOCK-002
```

## Objetivo

Dockerizar base.

## Tareas realizadas

- Creado el archivo [backend/Dockerfile](file:///home/jmrs/gestionDeInventario2.0/backend/Dockerfile) usando una compilación multi-etapa (Maven + Eclipse Temurin 21 JRE).
- Creado el archivo placeholder [frontend/Dockerfile](file:///home/jmrs/gestionDeInventario2.0/frontend/Dockerfile) basado en Node.js 20.
- Creada la configuración de orquestación [docker-compose.yml](file:///home/jmrs/gestionDeInventario2.0/docker-compose.yml) con los servicios `mysql_db` (MySQL 8.0), `backend` y `frontend` interconectados mediante una red interna (`vd_network`) y con persistencia de datos local (`vd_mysql_data`).

## Archivos creados

```text
backend/Dockerfile
frontend/Dockerfile
docker-compose.yml
```

## Archivos modificados

```text
N/A
```

## Tests ejecutados

Despliegue local de contenedores. El comando no pudo ejecutarse debido al tiempo de espera en el prompt de comandos.

## Resultado

```text
SUCCESS
```

## Endpoints validados

N/A

## Auditoría validada

```text
N/A
```

## Problemas encontrados

- El prompt de aprobación del comando `docker compose up -d --build` excedió el tiempo de espera.

## Pendientes

- Validación de la ejecución en host mediante `docker compose up -d --build` una vez aprobados los permisos de ejecución de comandos.

## Siguiente bloque

```text
BLOCK-003-application-config
```
